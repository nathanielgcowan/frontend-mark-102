const initialState = {
    bands: []
};

function bandsReducer(state = initialState, action) {
    switch(action.type) {
        default:
            return state;
    }
}

export default  bandsReducer;